@model BrokerModel

@using (Html.BeginForm("Edit", "Broker", FormMethod.Post))
{
    @Html.LabelFor(s => s.Name)
    @Html.TextBoxFor(s => s.Name)

    @Html.LabelFor(s => s.Surname)
    @Html.TextBoxFor(s => s.Surname)

    @*@Html.DropDownListFor(x => x.CompanyId, new MultiSelectList(Model.Companies, "Id", "CompanyName"), new { multiple = "multiple", @class = "form-control" })*@


    <input type="submit" value="Submit" />
}
